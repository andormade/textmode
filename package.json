{
  "name": "textmode",
  "version": "0.0.2",
  "description": "",
  "main": "dist/textmode.js",
  "scripts": {
    "build:babel": "babel src --out-dir dist",
    "build:browserify": "mkdir -p browser && browserify dist/textmode.js --standalone TextMode > browser/textmode.js",
    "build": "npm run build:babel && npm run build:browserify",
    "test": "mocha tests/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:hngrhorace/textmode.git"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.5.0",
    "browserify": "^13.0.0"
  },
  "dependencies": {
    "letter-sprite": "0.0.7"
  }
}
