{
  "name": "textmode",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build:babel": "babel src/js --out-dir dist",
    "build:browserify": "browserify dist/index.js --standalone TextMode > bundle/textmode.js",
    "build": "npm run build:babel && npm run build:browserify",
    "test": "mocha tests/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:hngrhorace/textmode.git"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-preset-es2015": "^6.5.0",
    "browserify": "^13.0.0"
  }
}
